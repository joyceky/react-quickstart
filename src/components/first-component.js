import React from 'react';

const FirstComponent = () => {
  // above arg is same as arg props plus const video = props.video;

  return (
    <h1>Hello from the FirstComponent</h1>
);
};

export default FirstComponent;
